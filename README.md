# Quantum Credit-Card Fraud Detection

Initial empty scaffold — the full implementation lands via PR.

See the open pull request for:
- Auto-detecting loader for both Kaggle credit-card datasets
- Preprocessing (StandardScaler + SMOTE + PCA)
- Classical baselines (LogReg / RandomForest / XGBoost / LightGBM)
- Variational Quantum Classifier (VQC) on Qiskit Aer
- Quantum-Kernel SVM (QSVC) + Pegasos QSVC
- Real IBM Quantum hardware submission template (Qiskit Runtime)
- Streamlit dashboard for the senior-management demo
- README + ARCHITECTURE + STEPS + slide outline with full ASCII diagrams
